from Clean import *
from classification import *



if __name__ == '__main__':
    # clean()
    #
    # x_train = pd.read_csv('scaled_train.csv').iloc[:, 1:]
    # x_test = pd.read_csv('scaled_test.csv').iloc[:, 1:]
    #
    # y_train = pd.read_csv('train_labels.csv').iloc[:, 1:]
    # y_test = pd.read_csv('test_labels.csv').iloc[:, 1:]
    #
    # prediction = Logistic_Regression(x_train, y_train, x_test, y_test)
